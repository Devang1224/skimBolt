export const generateSummary = async(length:string,language:string,tone:string,textContent:string)=>{
    try{

    }catch(err){
        console.error("Error generating summary: ",err);
        throw err;
    }
}